# Inkoku 1.0.1

Fix pencil marks stretching Sudoku cells. Board rows and columns now stay equal; all nine candidates fit inside each cell.

Verified unchanged cell dimensions with zero and nine marks at 320px and 390px phone widths. Signed APK built using the original key; install over 1.0.0 to retain progress. Android device runtime remains unverified.